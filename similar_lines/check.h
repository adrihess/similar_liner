#ifndef CHECK
#define CHECK

#include "structures.h"

extern bool isWhiteChar(char sign);
extern bool isForbidden(char sign);
extern bool isEnd(int sign) ;
extern bool isHex(char sign);

#endif